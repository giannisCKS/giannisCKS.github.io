# Style and conventions

- TypeScript strict mode is enabled (`tsconfig.json`).
- Codebase uses modern React/Next.js with function components and hooks.
- ESLint uses Next.js core-web-vitals + TypeScript presets via flat config (`eslint.config.mjs`).
- Existing code generally uses double quotes and semicolons.
