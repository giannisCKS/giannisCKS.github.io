# What to do when a task is completed

- Run `npm run lint` and `npm run typecheck` for quick feedback.
- Run `npm run test` when changes affect logic/components (or when explicitly requested).
- Keep changes minimal and aligned with existing Next.js App Router patterns.
